/**
 * Authentication Module — Part 3
 * File: src/components/auth/DemoCredentials.js
 *
 * Purpose:
 *   Read-only display block on the login page that shows the
 *   demo / test credentials so developers and evaluators can
 *   quickly log in without needing real accounts.
 *
 *   Displays:
 *     Email:    student@demo.com
 *     Password: demo123
 *
 * Key responsibilities (to be implemented):
 *   - Render the demo credentials in a clearly styled hint box
 *   - Optionally provide a "Use demo credentials" button that
 *     auto-fills the LoginForm inputs
 *   - Should be visually distinct (e.g. info/notice styling)
 *     but not distracting
 *
 * Dependencies (once implemented):
 *   - utils/constants.js  (DEMO_EMAIL, DEMO_PASSWORD)
 *
 * TODO: Implement demo credentials display block (AUTH-010,
 *       FR-AUTH-008).
 */

export {};
