export function createModuleProgress(completedModules, totalModules) {
    // Deprecated by two-column details layout in CourseCard.js
    // Keeping file to prevent import errors if used elsewhere
    return document.createElement('div');
}
